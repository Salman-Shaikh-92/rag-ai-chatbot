# RAG System Overview

This project uses Retrieval-Augmented Generation (RAG) to improve AI responses.

The system retrieves relevant information from a knowledge base before generating answers.

This approach reduces hallucinations and improves factual accuracy.

Workflow:

User Query → Document Retrieval → Context Injection → AI Response
